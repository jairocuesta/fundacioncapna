export type styles = {
    styles?: {
        background?: string;
        color?: string;
        position?: 'static' | 'relative' | 'absolute' | 'sticky' | 'fixed';
        image?: 'default' | 'color'
    }
}